syntax = "proto3";

package gravit.dwf.v1;

service IntegrationService {
  rpc RunAtoCPipeline(TestScenario) returns (stream string);
}

message TestScenario {
  string asset = 1;
  float expected_yield = 2;
}
